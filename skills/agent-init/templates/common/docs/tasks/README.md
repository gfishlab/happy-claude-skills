# tasks 目录说明

本目录存放当前执行型文档，例如联调步骤、验收清单、开发任务拆解。

使用规则：

- 一个 plan 可以对应多个 task 文件
- 任务要拆到 Agent 可以稳定执行的粒度
- 文件名使用稳定英文 slug，例如 `api-acceptance.md`、`frontend-phase-1-tasks.md`
- 不使用按年月日追加的文件名前缀
- 任务完成且无复用价值后直接删除
